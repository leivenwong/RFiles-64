WangPositionCost <-function(ms_name, type, direction, cost, percent) {
  # wang：你要实现一个这样的函数，并把这个函数从SERVER删除
  # 这个函数用来通知R，现在的持仓状况
  # 1 stringPosition.Cost
  # 2 string ms_name
  # 3 type
  # 3 方向 1 表示多仓 -1 表示空仓
  # 4 持仓成本
  # 5 配置文件中，品种的止损百分比
  if(ms_name == ms.successive.if.two)
  {
    IFdirection.two <<- direction
    IFcost.two <<- cost
    IFpercent.two <<- percent
  }
}